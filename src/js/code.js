function choosePlatform(parameter)
{
    switch(parameter)
    {
        case "Party":
            // code block
            break;
        case "Match":            
            // code block
            break;
        case "Sream":
            // code block
            break;
    }
}